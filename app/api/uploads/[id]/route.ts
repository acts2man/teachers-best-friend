import {owner,database,bucket,guardOrigin,apiError,HttpError} from "@/lib/teacher-server";
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const user=await owner(),{id}=await params;const row=await database().prepare("SELECT object_key,name,mime FROM teacher_uploads WHERE id=? AND owner_id=?").bind(id,user).first<{object_key:string;name:string;mime:string}>();if(!row)throw new HttpError(404,"Document not found.");const object=await bucket().get(row.object_key);if(!object)throw new HttpError(404,"Document not found.");return new Response(object.body,{headers:{"Content-Type":row.mime,"Content-Disposition":"inline; filename*=UTF-8''"+encodeURIComponent(row.name),"Cache-Control":"private, no-store","X-Content-Type-Options":"nosniff"}})}catch(e){return apiError(e)}}
export async function DELETE(request:Request,{params}:{params:Promise<{id:string}>}){try{guardOrigin(request);const user=await owner(),{id}=await params;const row=await database().prepare("SELECT object_key FROM teacher_uploads WHERE id=? AND owner_id=?").bind(id,user).first<{object_key:string}>();if(!row)throw new HttpError(404,"Document not found.");await bucket().delete(row.object_key);await database().prepare("DELETE FROM teacher_uploads WHERE id=? AND owner_id=?").bind(id,user).run();return Response.json({ok:true})}catch(e){return apiError(e)}}
