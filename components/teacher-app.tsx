"use client";
import {useEffect,useRef,useState} from "react";
import Link from "next/link";
import {useRouter} from "next/navigation";
import {House,ScanLine,Files,ClipboardCheck,Lightbulb,Users,Library,Settings,ArrowRight,Plus,ChevronDown,HelpCircle,Check,LoaderCircle,ShieldCheck,BookOpen} from "lucide-react";
import {SidebarProvider,Sidebar,SidebarHeader,SidebarContent,SidebarFooter,SidebarMenu,SidebarMenuItem,SidebarMenuButton,SidebarTrigger} from "@/components/ui/sidebar";
import {Toaster,toast} from "sonner";
import {catalogFor} from "@/lib/teacher-catalog";
import {createDemoWorkspace,standards} from "@/lib/teacher-data";
import type {Workspace} from "@/lib/teacher-types";
import {TeacherContext} from "./teacher-context";
import {Pick,Action,Modal,Pill} from "./teacher-shared";
import HomeView from "./teacher-home";
import {ReviewWorkView} from "./teacher-review";
import {AssessmentView} from "./teacher-assessments";
import {ScanView} from "./teacher-scan";
import {StandardsView,DiagnosticsView,StudentsView} from "./teacher-insights";
import {ReteachView,ResourcesView,SettingsView} from "./teacher-planning";
const nav=[{id:"home",label:"Overview",icon:House},{id:"assessments",label:"Assignments",icon:Files},{id:"review",label:"Student work",icon:ClipboardCheck},{id:"reteach",label:"Reteach",icon:BookOpen},{id:"students",label:"Students",icon:Users}];
const libraryNav=[{id:"standards",label:"Standards",icon:Library}];
export default function TeacherApp({view}:{view:string}){
const [w,setW]=useState<Workspace>(createDemoWorkspace);const [revision,setRevision]=useState(0);const [loaded,setLoaded]=useState(false);const [busy,setBusy]=useState(false);const lock=useRef(false);const [aiReady,setAiReady]=useState(false);const [error,setError]=useState("");const [createOpen,setCreateOpen]=useState(false);const [help,setHelp]=useState(false);const [name,setName]=useState("");const [grade,setGrade]=useState("4");const router=useRouter();
async function reload(){try{setError("");const r=await fetch("/api/workspace",{cache:"no-store"});const d=await r.json();if(!r.ok)throw new Error(d.error);setW(d.workspace);setRevision(d.revision);setAiReady(d.aiReady);setLoaded(true)}catch(e){setError(e instanceof Error?e.message:"Your classroom couldn’t be loaded.")}}
useEffect(()=>{reload()},[]);
useEffect(()=>{document.documentElement.classList.toggle("reduce-motion",w.settings.reduceMotion)},[w.settings.reduceMotion]);
async function save(next:Workspace,message?:string){if(!loaded){toast.error("Wait for your classroom to load before saving.");return false}if(lock.current)return false;lock.current=true;setBusy(true);try{const r=await fetch("/api/workspace",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({workspace:next,revision})});const d=await r.json();if(!r.ok)throw new Error(d.error);setW(next);setRevision(d.revision);if(message)toast.success(message);return true}catch(e){toast.error(e instanceof Error?e.message:"Changes couldn’t be saved.");return false}finally{setBusy(false);lock.current=false}}
const classroom=w.classes.find(c=>c.id===w.activeClassId)||w.classes[0],students=w.students.filter(s=>s.classId===classroom.id),assessments=w.assessments.filter(a=>a.classId===classroom.id),catalog=catalogFor(w,classroom.grade,classroom.framework);
const value={w,classroom,students,assessments,catalog,loaded,busy,aiReady,save,reload,go:(url:string)=>router.push(url)};
const title=[...nav,...libraryNav,{id:"scan",label:"Scan assignment"},{id:"settings",label:"Classroom settings"}].find(n=>n.id===view)?.label||"Overview";
async function createClass(){if(!name.trim())return;const id=crypto.randomUUID();if(await save({...w,classes:[...w.classes,{id,name:name.trim(),grade:Number(grade),framework:"California",demo:false}],activeClassId:id},"Your classroom is ready")){setCreateOpen(false);setName("");router.push("/")}}
return <TeacherContext.Provider value={value}><SidebarProvider style={{"--sidebar-width":"238px"} as React.CSSProperties} className={w.settings.reduceMotion?"reduce-motion":""}><Sidebar className="app-sidebar"><SidebarHeader className="brand-area"><Link href="/" className="brand"><span className="brand-mark"><img src="/brand/teacher-book.png" alt="" width="44" height="44"/></span><span>a teacher’s<span>best friend<span className="brand-dot">.</span></span></span></Link></SidebarHeader><SidebarContent className="side-content"><button className="new-scan" onClick={()=>router.push("/scan")}><ScanLine size={18}/>New assignment<Plus size={16}/></button><div className="nav-label">YOUR WORKSPACE</div><SidebarMenu>{nav.map(n=><SidebarMenuItem key={n.id}><SidebarMenuButton asChild isActive={view===n.id} className="nav-link"><Link href={n.id==="home"?"/":"/"+n.id}><n.icon size={19}/><span>{n.label}</span>{view===n.id&&<span className="nav-active-dot"/>}</Link></SidebarMenuButton></SidebarMenuItem>)}</SidebarMenu><div className="nav-label library-label">YOUR TOOLKIT</div><SidebarMenu>{libraryNav.map(n=><SidebarMenuItem key={n.id}><SidebarMenuButton asChild isActive={view===n.id} className="nav-link"><Link href={"/"+n.id}><n.icon size={18}/><span>{n.label}</span></Link></SidebarMenuButton></SidebarMenuItem>)}</SidebarMenu></SidebarContent><SidebarFooter className="side-footer"><button className="footer-link" onClick={()=>setHelp(true)}><HelpCircle size={18}/>How it works</button><Link href="/settings" className="footer-link"><Settings size={18}/>Settings</Link><div className="teacher-account"><span className="account-avatar">{(w.settings.teacherName||"Teacher").slice(0,1)}</span><div><strong>{w.settings.teacherName||"Your teaching space"}</strong><span>Personal workspace</span></div><ShieldCheck size={16}/></div></SidebarFooter></Sidebar><div className="app-main"><header className="topbar"><div className="breadcrumb"><SidebarTrigger className="mobile-menu"/><span>My classroom</span><span className="breadcrumb-slash">/</span><strong>{title}</strong></div><div className="topbar-right">{loaded&&<span className="save-indicator">{busy?<LoaderCircle size={14} className="spin"/>:<Check size={14}/>}<span>{busy?"Saving":"Saved"}</span></span>}<div className="class-switch"><Pick label="Choose classroom" value={classroom.id} onChange={v=>v==="new"?setCreateOpen(true):save({...w,activeClassId:v})} options={[...w.classes.map(c=>({value:c.id,label:c.name+" · Grade "+c.grade})),{value:"new",label:"+ New classroom"}]}/></div></div></header><main className="workspace-content" id="main-content">{error&&<div className="error-banner" role="alert"><span>{error}</span><button onClick={reload}>Retry</button></div>}{classroom.demo&&<div className="demo-strip"><Pill tone="green">SAMPLE CLASSROOM</Pill><span>Meet the Explorers. All student names and results are fictional.</span><button onClick={()=>setCreateOpen(true)}>Make it yours<ArrowUpRightIcon/></button></div>}<div className="view-enter" key={view+"-"+classroom.id}>{view==="home"?<HomeView/>:view==="assessments"?<AssessmentView/>:view==="review"?<ReviewWorkView/>:view==="scan"?<ScanView/>:view==="standards"?<StandardsView/>:view==="diagnostics"?<DiagnosticsView/>:view==="students"?<StudentsView/>:view==="reteach"?<ReteachView/>:view==="resources"?<ResourcesView/>:<SettingsView/>}</div></main></div></SidebarProvider><Toaster position="bottom-right" richColors closeButton/><Modal open={createOpen} onClose={()=>setCreateOpen(false)} title="Create your classroom" description="Add your students and choose your first assignment."><form onSubmit={e=>{e.preventDefault();createClass()}} className="form-stack"><label>Classroom name<input required maxLength={70} value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. Room 12 · Morning class"/></label><label>Grade<Pick label="Grade" value={grade} onChange={setGrade} options={Array.from({length:13},(_,i)=>({value:String(i),label:i===0?"Kindergarten":"Grade "+i}))}/></label><p className="field-help">California Grade 4 Math and ELA standards are included. Other grades can use standards you add to the library.</p><Action type="submit" disabled={busy||!name.trim()}>Create classroom<ArrowRight size={16}/></Action></form></Modal><Modal open={help} onClose={()=>setHelp(false)} title="From assignment to reteaching" description="Follow one assignment from standards to student support."><div className="help-steps">{["Choose the grade, subject, and standards you want to assess.","Upload the assignment, check its alignment, and confirm the answer key.","Photograph one student’s work. Review flagged answers and approve the clear ones together.","Choose a visual, hands-on, or auditory approach for the skill that needs support."].map((x,i)=><div key={x}><span>{i+1}</span><p>{x}</p></div>)}</div><div className="insight"><ShieldCheck/><p>Start with student aliases. You control corrections, exports, and deletion in your private workspace.</p></div></Modal></TeacherContext.Provider>
}
function ArrowUpRightIcon(){return <ArrowRight size={14}/>}
